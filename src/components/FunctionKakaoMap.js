import React from 'react';
/*
 1). 클래스의 생성자에 해당하는 내용을 함수형에서는 어떻게 변수를 초기화 하는지 이해 할 수 있다.
     (예,this.state = { keyword: '성남시',}의 this객체 대신에 함수형은 var [keyword, setKeyword] = useState('성남시'); useState함수를 사용
 2). 클래스의 렌더함수에 해당하는 내용을 함수형에서는 return()함수만으로 어떻게 적용하는지 이해 할 수 있다.
 3). 클래스의 렌더링 이후 실행되는 componentDidMount()함수를 함수형에서는 useEffect()함수(Hook함수)로 어떻게 적용하는지 이해 할 수 있다.
*/
function FunctionKakaoMap(props) {
    return (
        <div>
            
        </div>
    );
}

export default FunctionKakaoMap;